export type WideKeyword = "initial" | "inherit" | "revert" | "unset";

export type CustomIndent = string;
export type DashedIndent = `--${string}`;

export type String = `"${string}"` | `'${string}'`;

export type Url = `url(${string})`;
