{
  "name": "cssmediaqueries",
  "version": "0.1.0",
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo clean",
    "dev": "turbo dev --parallel",
    "format": "turbo format --continue -- --cache --cache-location node_modules/.cache/.formatcache",
    "format:fix": "turbo format --continue -- --write --cache --cache-location node_modules/.cache/.formatcache",
    "lint": "turbo lint --continue -- --cache --cache-location node_modules/.cache/.lintcache",
    "lint:fix": "turbo lint --continue -- --fix --cache --cache-location node_modules/.cache/.lintcache",
    "lint:ws": "pnpm dlx sherif@latest",
    "postinstall": "pnpm lint:ws",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@turbo/gen": "^2.1.3",
    "typescript": "^5.6.2"
  }
}
